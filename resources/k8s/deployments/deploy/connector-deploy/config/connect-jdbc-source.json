{
  "name": "postgres_cdc_source",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSourceConnector",
    "tasks.max": "1",
    "connection.url": "jdbc:postgresql://tc-showcase-test-psql.postgres.database.azure.com:5432/orderdomain_db",
    "connection.user": "${azure:vault-tc-showcase-test.vault.azure.net:database-user}",
    "connection.password": "${azure:vault-tc-showcase-test.vault.azure.net:database-user}",
    "mode": "incrementing",
    "incrementing.column.name": "version",
    "topic.prefix": "tc_showcase_source"
  }
}
